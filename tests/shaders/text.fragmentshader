#version 330 core

in vec2 frag_uv;
out vec4 color;

uniform sampler2D textureSampler;

void main(){
    color = vec4(1.0, 0.0, 0.0, texture(textureSampler, frag_uv).r);
    //color = vec4(1.0, 1.0, 0.0, 1.0);
    //color.xyzw = vec4(0.0, 1.0, 0.0, 1.0);
}