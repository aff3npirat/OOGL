#version 330 core

layout(location = 0) in vec2 position;
layout(location = 1) in vec2 uv;
out vec2 frag_uv;

uniform mat4 P;

void main(){
    gl_Position = P * vec4(position, 0.0, 1.0);
    frag_uv = uv;
}